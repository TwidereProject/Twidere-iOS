imports:
  - Foundation
submodels:
  UserList:
    fields:
      accountKey:
        type: UserKey
        column: account_key
        json_field: account_key
        json_transform: UserKeyTransform
